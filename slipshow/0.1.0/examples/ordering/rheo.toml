# Every `order:` form `#slipshow` accepts, compared side by side. See
# `examples/_template/rheo.toml` for what each line below means and why the
# `[packages.rookery]` override exists — copied verbatim, not reinterpreted.
version = "0.6.2"
content_dir = "content"
formats = ["html"]

[spine]
exclude = ["lib.typ"]

[packages.rookery]
path = "../../../.."
