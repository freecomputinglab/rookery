# Design notes

Design notes about how parts of `@rookery/core` work, kept here rather than
in the package readme because they run longer than a package reader needs.

- `generating-idea-names.md` — the algorithm `@rookery/core` uses to name a
  note that was not explicitly named.
